@model CommentModel

@{
    ViewData["Title"] = "Edit Comment";
    var todoListId = TempData["TodoListId"] as int?;
    var taskId = TempData["TaskId"] as int?;
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">Edit Comment</div>
                <div class="card-body">
                    <form asp-action="EditComment" method="post">
                        <input type="hidden" asp-for="Id" />
                        <input type="hidden" asp-for="TaskId" />
                        <div class="form-group mb-3">
                            <label asp-for="Text" class="form-label">Comment</label>
                            <textarea asp-for="Text" class="form-control"></textarea>
                            <span asp-validation-for="Text" class="text-danger"></span>
                        </div>

                        <button type="submit" class="btn btn-primary">Save</button>
                        <a class="btn btn-outline-secondary" asp-action="Details" asp-route-id="@Model.TaskId" asp-route-todoListId="@todoListId">Cancel</a>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}